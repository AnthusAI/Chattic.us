{
  "schema_version": 1,
  "event_id": "19405800-62d6-4f63-8228-38e23c17e76b",
  "issue_id": "chatticus-71e3e06b-ea10-43a8-8323-dd18d0bef0e0",
  "event_type": "field_updated",
  "occurred_at": "2026-09-01T02:46:57.800Z",
  "actor_id": "home",
  "payload": {
    "changes": {
      "description": {
        "from": "People reach hey.chattic.us only as an authenticated principal. Cognito user pool per environment with Google as the sole social provider; the SPA does authorization code with PKCE; the front door verifies the Cognito JWT and resolves a Principal. X-Tenant-Id is deleted from the client. Routing is default deny: a route serves an enabled member unless it is explicitly marked reachable by a waitlisted one. Includes the OpenAI spend cap and AWS budget alarm, which land first and cover development while the rest is built. Includes per-worker credentials, closing the open question at docs/DESIGN_CHALLENGES.md.",
        "to": "Spans phases 1, 2, 3, and 4. Ends at the join, 7b4616, where enforcement flips on.\n\nPeople reach hey.chattic.us only as an authenticated principal. Cognito user pool per environment with Google as the sole social provider; the SPA does authorization code with PKCE; the front door verifies the Cognito JWT and resolves a Principal. X-Tenant-Id is deleted from the client. Routing is default deny: a route serves an enabled member unless it is explicitly marked reachable by a waitlisted one.\n\nAlso holds the two controls that start before anything else: the spend alarm 79ca6a, and taking staging and production dark in 7c1382. Per-worker credentials, fac2ee, close the question left open in docs/DESIGN_CHALLENGES.md.\n\n**Start with:** 79ca6a, 7c1382, 0ab02c, and 1ebc7d, all in parallel, today."
      }
    }
  }
}